import { Card, CardContent, Typography } from "@mui/material";

export default function Student() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h5">Student Dashboard</Typography>
        <Typography>Your certificates are secure on blockchain</Typography>
      </CardContent>
    </Card>
  );
}